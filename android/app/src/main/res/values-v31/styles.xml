<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+ (API 31) uses the native SplashScreen API -->
    <!-- This ensures that the system-level splash screen has our brand purple background
         and the Cup Tales logo, not a generic white or black screen. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <!-- The background color of the splash screen -->
        <item name="android:windowSplashScreenBackground">@color/brand_purple</item>
        
        <!-- The icon to display in the center -->
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_logo</item>
        
        <!-- Colors to prevent white flashes during transition -->
        <item name="android:statusBarColor">@color/brand_purple</item>
        <item name="android:navigationBarColor">@color/brand_purple</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@color/brand_purple</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@color/brand_purple</item>
        <item name="android:navigationBarColor">@color/brand_purple</item>
    </style>
</resources>
