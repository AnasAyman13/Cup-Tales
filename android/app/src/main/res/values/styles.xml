<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- LaunchTheme: shown while the Dart VM starts and Flutter engine loads -->
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@color/brand_purple</item>
        <item name="android:navigationBarColor">@color/brand_purple</item>
    </style>

    <!-- NormalTheme: shown between Flutter engine ready and first Flutter frame.
         MUST use brand_purple to avoid a white flash at this transition. -->
    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@color/brand_purple</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@color/brand_purple</item>
        <item name="android:navigationBarColor">@color/brand_purple</item>
    </style>
</resources>
